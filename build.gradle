apply plugin: 'java'
apply plugin: 'maven'

group = 'com.squareup.retrofit'
version = '1.9.0'

sourceCompatibility = 1.6
targetCompatibility = 1.6

repositories {
    jcenter();
}

task wrapper(type: Wrapper)
